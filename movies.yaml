movie:
  directory: /home/spark/Documentos/repositorios/spring-integration
  file-pattern: "*.txt"
  prevent-duplicates: true
  fixed-delay: 5000

eureka:
  client:
    serviceUrl.defaultZone: http://localhost:8010/eureka
